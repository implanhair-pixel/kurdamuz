# PROJECT ISSUES REPORT — KurdAmuz (KurdHub v2)

> **Generated:** 2026-06-16  
> **Scope:** Full static analysis (TypeScript compilation, dependency audit, import/export validation, route structure, environment config)

---

## Summary

| Category | Count |
|---|---|
| TypeScript Errors | ~400+ |
| Critical Severity | 12 |
| High Severity | 24 |
| Medium Severity | 18 |
| Low Severity | 8 |
| Missing Environment Variables | 12 |
| Deprecated Dependencies | 3 |
| Security Vulnerabilities (npm audit) | 26 (23 moderate, 2 high, 1 critical) |

---

## 1. Missing Environment Variables

### 1.1 No `.env` File Found ⚠️ Critical

| Field | Value |
|---|---|
| **File** | `/.env` |
| **Exact Error** | File does not exist. `Test-Path` returns `False`. |
| **Root Cause** | Project was cloned/copied without creating `.env` from `.env.example`. All 12 required env vars are undefined. |
| **Severity** | **Critical** |
| **Fix Difficulty** | Easy (copy `.env.example` → `.env` and fill values) |

### 1.2 Required Environment Variables

| Variable | Defined in .env.example | Status |
|---|---|---|
| `DATABASE_URL` | ✅ | **MISSING** — App crashes at `src/db/index.ts:5` |
| `DIRECT_URL` | ✅ | **MISSING** |
| `NEXT_PUBLIC_SUPABASE_URL` | ✅ | **MISSING** — `src/lib/supabase.ts:3` uses `!` assertion |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ✅ | **MISSING** — `src/lib/supabase.ts:4` uses `!` assertion |
| `SUPABASE_SERVICE_ROLE_KEY` | ✅ | **MISSING** — `src/lib/supabase.ts:10` uses `!` assertion |
| `NEXT_PUBLIC_APP_URL` | ✅ | **MISSING** |
| `NEXT_PUBLIC_SITE_URL` | ✅ | **MISSING** |
| `CRON_SECRET` | ✅ | **MISSING** — `src/lib/cron-validation.ts` will fail |
| `CSRF_SECRET` | ✅ | **MISSING** — `src/lib/csrf.ts` will fail |
| `GLITCHTIP_DSN` | ✅ | **MISSING** — Sentry init will fail |
| `NEXT_PUBLIC_POSTHOG_KEY` | ✅ | **MISSING** |
| `NEXT_PUBLIC_POSTHOG_HOST` | ✅ | **MISSING** |

---

## 2. TypeScript Compilation Errors

### 2.1 Next.js 15 — `params` Must Be `Promise` (Auto-generated .next/types) ⚠️ Critical

| Field | Value |
|---|---|
| **Files** | All `.next/types/app/**/page.ts` and `.next/types/app/**/route.ts` files |
| **Exact Error** | `Type '{ params: { id: string; }; }' does not satisfy the constraint 'PageProps'. Types of property 'params' are incompatible. Type '{ id: string; }' is missing the following properties from type 'Promise<any>': then, catch, finally, [Symbol.toStringTag]` |
| **Root Cause** | Next.js 15 changed `params` and `searchParams` to be `Promise`-based. Generated types expect `params: Promise<{...}>` but source files use synchronous `params: { id: string }`. Over **150 files** auto-generated in `.next/types/` are affected. |
| **Severity** | **Critical** (blocks build, affects all dynamic route pages and API routes) |
| **Fix Difficulty** | Medium — requires updating all page/route handler signatures to use `params: Promise<{...}>` and `await params` |

**Affected routes (sample):**
- `.next/types/app/[locale]/community/[id]/page.ts`
- `.next/types/app/[locale]/stories/[slug]/page.ts`
- `.next/types/app/api/admin/courses/[id]/route.ts`
- `.next/types/app/api/community/comments/[id]/route.ts`
- `.next/types/app/api/dialects/[id]/route.ts`
- `.next/types/app/api/notebooks/[id]/entries/route.ts`
- `.next/types/app/api/placement/admin/questions/[id]/route.ts`
- And ~30+ more API route handler files

### 2.2 Type Mismatch — Drizzle DB `null` vs TypeScript `undefined` ⚠️ Critical

| Field | Value |
|---|---|
| **Files** | `src/lib/dialects/dialect-service.ts`, `src/lib/comparison/lexical-comparator.ts`, `src/types/dialects.ts` |
| **Exact Errors** | `Type 'string | null' is not assignable to type 'string | undefined'` (28 occurrences in `dialect-service.ts`, 5 in `lexical-comparator.ts`) |
| **Root Cause** | Drizzle ORM columns with `text(...)` (no `.notNull()`) return `string | null` from queries. But the type definitions in `src/types/dialects.ts` define these properties as `string | undefined`. The `null` vs `undefined` mismatch affects: `description`, `phoneticForm`, `semanticDomain`, `annotationType`, `relationshipType`. |
| **Severity** | **Critical** |
| **Fix Difficulty** | Medium — either change Drizzle schema to add `.notNull()` or change type definitions to accept `null` |

**Affected types:**
- `Dialect.description` — DB returns `string | null`, type says `string | undefined`
- `DialectVariant.phoneticForm` — DB returns `string | null`, type says `string | undefined`
- `LexicalMeaning.semanticDomain` — DB returns `string | null`, type says `string | undefined`
- `LinguisticAnnotation.annotationType` — DB returns `string`, type expects union string literal

### 2.3 Missing Imports in `src/lib/challenges/scoring.ts` ⚠️ High

| Field | Value |
|---|---|
| **File** | `src/lib/challenges/scoring.ts:185-211` |
| **Exact Error** | `Cannot find name 'challengeLeaderboards'` (6 occurrences) |
| **Root Cause** | The table `challengeLeaderboards` is defined in `src/db/schema.ts` but is not imported or not accessible in `scoring.ts`. The code references it directly without an import. |
| **Severity** | **High** |
| **Fix Difficulty** | Easy — add `import { challengeLeaderboards } from '@/db/schema'` |

### 2.4 Missing Imports in `src/lib/community/integration.ts` ⚠️ High

| Field | Value |
|---|---|
| **File** | `src/lib/community/integration.ts:154,176` |
| **Exact Error** | `Cannot find name 'sql'` (2 occurrences) |
| **Root Cause** | The file references `sql` template literal (for Drizzle) without importing it from `drizzle-orm`. |
| **Severity** | **High** |
| **Fix Difficulty** | Easy — add `import { sql } from 'drizzle-orm'` |

### 2.5 Missing Exports in `src/lib/xp/progression.ts` ⚠️ High

| Field | Value |
|---|---|
| **File** | `src/lib/community/integration.ts:11` |
| **Exact Error** | `Module '"@/lib/xp/progression"' has no exported member 'addXP'` |
| **Root Cause** | The `integration.ts` file imports `addXP` from `progression.ts`, but that function was renamed or removed from the progression module. |
| **Severity** | **High** |
| **Fix Difficulty** | Medium — check `progression.ts` for the actual function name and update the import |

### 2.6 Missing Exports in `src/types/dialects.ts` ⚠️ High

| Field | Value |
|---|---|
| **File** | `src/lib/audit/audit-logger.ts:4` |
| **Exact Error** | `Module '"@/types/dialects"' has no exported member 'AuditLogEntry'` and `'CreateAuditLogRequest'` |
| **Root Cause** | The types file doesn't define `AuditLogEntry` or `CreateAuditLogRequest` that `audit-logger.ts` depends on. |
| **Severity** | **High** |
| **Fix Difficulty** | Medium — add the missing type definitions or fix the import |

### 2.7 Schema/Audit Logger Mismatch ⚠️ High

| Field | Value |
|---|---|
| **File** | `src/lib/audit/audit-logger.ts:21-57` |
| **Exact Error** | `Object literal may only specify known properties, and 'userId' does not exist in type...` (3 occurrences) |
| **Root Cause** | The audit logger tries to insert fields (`entityType`, `entityId`, `userId`) into the `dialect_audit_logs` table which has columns: `actorId`, `actionType`, `targetId`, `oldValue`, `newValue`, `createdAt`. The Drizzle schema doesn't have `entityType`, `entityId`, or `userId` columns. |
| **Severity** | **High** |
| **Fix Difficulty** | Medium — either update Drizzle schema or fix the logger to use correct column names |

### 2.8 Missing `and` Import in `src/lib/curriculum/structure.ts` ⚠️ Medium

| Field | Value |
|---|---|
| **File** | `src/lib/curriculum/structure.ts:191,214` |
| **Exact Error** | `Cannot find name 'and'` |
| **Root Cause** | Uses `and(...)` for Drizzle where conditions without importing it. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Easy — add `import { and } from 'drizzle-orm'` |

### 2.9 Drizzle Insert Type Mismatch — `number` vs `string` for `scheduledDate` ⚠️ Medium

| Field | Value |
|---|---|
| **File** | `src/lib/challenges/scheduler.ts:136` |
| **Exact Error** | `Type 'Date' is not assignable to type 'string | SQL<unknown> | Placeholder<string, any>'` |
| **Root Cause** | Drizzle's `date('scheduled_date')` expects a string (or SQL), not a Date object. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Easy — use `.toISOString()` or format the date as string |

### 2.10 `percentile` Column Does Not Exist on `challengeLeaderboards` ⚠️ High

| Field | Value |
|---|---|
| **File** | `src/lib/challenges/leaderboard.ts:42,59` |
| **Exact Error** | `Object literal may only specify known properties, and 'percentile' does not exist in type...` |
| **Root Cause** | The `challengeLeaderboards` table schema in `src/db/schema.ts` doesn't have a `percentile` column, but the code tries to insert it. |
| **Severity** | **High** |
| **Fix Difficulty** | Medium — either add the column to the schema or remove it from the insert |

### 2.11 `coins` Column Does Not Exist on `userLevels` ⚠️ High

| Field | Value |
|---|---|
| **File** | `src/lib/community/integration.ts:154,176` |
| **Exact Error** | `Object literal may only specify known properties, and 'coins' does not exist in type...` |
| **Root Cause** | The code tries to update `user_levels.coins` but the Drizzle schema for `userLevels` table only has: `id`, `userId`, `currentLevel`, `currentXP`, `totalXP`, `xpToNextLevel`, `updatedAt`. There is no `coins` column. |
| **Severity** | **High** |
| **Fix Difficulty** | Medium — either add a `coins` column to `userLevels` or route coin updates to the wallet table |

### 2.12 `db.fn` Does Not Exist on PostgreSQL Driver ⚠️ Medium

| Field | Value |
|---|---|
| **File** | `src/lib/community/moderation.ts:183` |
| **Exact Error** | `Property 'fn' does not exist on type 'PostgresJsDatabase<...>'` |
| **Root Cause** | The code tries to call `db.fn(...)` which doesn't exist on the Drizzle Postgres.js driver. This is likely a method from a different database driver. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Medium — check what `fn` was supposed to do and use the correct Drizzle API |

### 2.13 Type Mismatch — `number` vs `string` in `challengeScores` Insert ⚠️ Medium

| Field | Value |
|---|---|
| **File** | `src/lib/challenges/scoring.ts:143` |
| **Exact Error** | `Type 'number' is not assignable to type 'string | SQL<unknown> | Placeholder<string, any> | undefined'` |
| **Root Cause** | Drizzle's `numeric` columns are typed as `string` in JS. The scoring code passes a `number` directly. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Easy — convert numbers to strings with `.toString()` |

---

## 3. Dependency & Security Issues

### 3.1 Next.js Security Vulnerability ⚠️ Critical

| Field | Value |
|---|---|
| **File** | `package.json` |
| **Exact Error** | `npm warn deprecated next@15.0.0: This version has a security vulnerability. Please upgrade to a patched version. See https://nextjs.org/blog/CVE-2025-66478` |
| **Root Cause** | Running Next.js 15.0.0 which has a known CVE. |
| **Severity** | **Critical** |
| **Fix Difficulty** | Easy — upgrade to latest Next.js 15.x patch |

### 3.2 Deprecated `@supabase/auth-helpers-nextjs` ⚠️ Medium

| Field | Value |
|---|---|
| **File** | `package.json` (dependency) |
| **Exact Error** | `npm warn deprecated @supabase/auth-helpers-nextjs@0.10.0: This package is now deprecated - please use the @supabase/ssr package instead.` |
| **Root Cause** | The legacy auth helper is still a dependency (though `@supabase/ssr` is also installed). The code in `src/lib/auth.ts` correctly uses `@supabase/ssr`, so the old package is unused but still in package.json. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Easy — remove the deprecated package |

### 3.3 Deprecated `@supabase/auth-helpers-shared` ⚠️ Low

| Field | Value |
|---|---|
| **File** | `package.json` (transitive dependency) |
| **Exact Error** | `This package is now deprecated - please use the @supabase/ssr package instead.` |
| **Root Cause** | Pulled in transitively by `@supabase/auth-helpers-nextjs`. Will be removed when that package is removed. |
| **Severity** | **Low** |
| **Fix Difficulty** | Easy — removed automatically when the parent package is removed |

### 3.4 Deprecated ESLint Packages ⚠️ Low

| Field | Value |
|---|---|
| **File** | `package.json` (devDependencies) |
| **Exact Error** | `npm warn deprecated eslint@8.57.1: This version is no longer supported.` |
| **Root Cause** | ESLint 8.x is EOL; also `@humanwhocodes/config-array@0.13.0`, `@esbuild-kit/core-utils`, `@esbuild-kit/esm-loader` are deprecated. |
| **Severity** | **Low** |
| **Fix Difficulty** | Medium — requires upgrading to ESLint 9.x and flat config |

### 3.5 npm Audit — 26 Vulnerabilities ⚠️ High

| Field | Value |
|---|---|
| **File** | `package-lock.json` |
| **Exact Error** | `26 vulnerabilities (23 moderate, 2 high, 1 critical)` |
| **Root Cause** | Multiple transitive dependencies with known vulnerabilities. |
| **Severity** | **High** |
| **Fix Difficulty** | Medium — run `npm audit fix` (may require `--force` for breaking changes) |

---

## 4. Missing Pages & Route Structure Issues

### 4.1 Missing `[locale]/layout.tsx` ⚠️ Critical

| Field | Value |
|---|---|
| **Path** | `src/app/[locale]/layout.tsx` |
| **Exact Error** | File does **not exist**. The project uses `next-intl` with internationalized routing but the locale layout is missing. |
| **Root Cause** | The `src/app/layout.tsx` exists at root level, but `next-intl` with the `as-needed` locale prefix strategy requires a `[locale]/layout.tsx` to provide the locale context, NextUI provider, and locale-specific HTML attributes. |
| **Severity** | **Critical** (app will not render locale-specific content; all pages under `src/app/[locale]/` will 404) |
| **Fix Difficulty** | Medium — create the missing layout file with proper providers |

### 4.2 Missing `not-found.tsx` Pages ⚠️ Medium

| Field | Value |
|---|---|
| **Path** | Various route segments |
| **Exact Error** | No custom `not-found.tsx` exists anywhere in the `src/app/` directory tree. |
| **Root Cause** | The middleware and `next-intl` `notFound()` calls in `src/i18n/request.ts` will trigger default 404 with no custom UX. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Easy — create `not-found.tsx` at app root and `[locale]/not-found.tsx` |

### 4.3 Missing `error.tsx` Error Boundaries ⚠️ Medium

| Field | Value |
|---|---|
| **Path** | `src/app/error.tsx` and all `[locale]/error.tsx` |
| **Exact Error** | No Next.js error boundary files exist. |
| **Root Cause** | Without error boundaries, unhandled errors will show the default Next.js error page in production. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Easy — create standard error boundary components |

---

## 5. DB Schema & Migration Issues

### 5.1 Inconsistent `dialect_audit_logs` Schema Usage ⚠️ High

| Field | Value |
|---|---|
| **File** | `src/db/schema.ts:327-335` vs `src/lib/audit/audit-logger.ts` |
| **Exact Error** | Schema defines columns: `id`, `actorId`, `actionType`, `targetId`, `oldValue`, `newValue`, `createdAt`. Code tries to insert: `entityType`, `entityId`, `userId`. |
| **Root Cause** | Different developers created the table schema and the service code without coordination. The audit logger expects a `challenge_audit_logs`-style schema but is trying to write to `dialect_audit_logs`. |
| **Severity** | **High** |
| **Fix Difficulty** | Medium — align the audit logger with the actual schema columns |

### 5.2 Migration `drizzle/0002_*` Duplicate Numbering ⚠️ Medium

| Field | Value |
|---|---|
| **Path** | `drizzle/` |
| **Exact Error** | There are **4 different migrations** all numbered `0002`: `0002_phase7_streaks_achievements.sql`, `0002_phase8_daily_challenges.sql`, `0002_phase9_wallet_system.sql`, `0002_phase11_learning_paths.sql` |
| **Root Cause** | Multiple phases were developed in parallel and all numbered `0002`, creating ambiguity in migration order. The migration tool will not apply them correctly. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Hard — requires renumbering migrations 0003-0019 with correct sequential numbering |

### 5.3 Migration Numbering Gap ⚠️ Low

| Field | Value |
|---|---|
| **Path** | `drizzle/` |
| **Exact Error** | Migrations jump from `0006` to `0010`, `0012`, `0015`, `0016`, `0017`, `0018`, `0019` — numbers 0007-0009, 0011, 0013-0014 are missing. |
| **Root Cause** | Phases were developed independently and merged without renumbering. |
| **Severity** | **Low** (Drizzle handles this but it's confusing) |
| **Fix Difficulty** | Hard — rename all migration files with sequential numbering |

---

## 6. Import/Export Audit Issues

### 6.1 Unused Imports in `src/app/layout.tsx` ⚠️ Low

| Field | Value |
|---|---|
| **File** | `src/app/layout.tsx` |
| **Exact Error** | Imports `Script` from `next/script` and uses it for web-vitals tracking, but the web-vitals external script URL (`cdn.jsdelivr.net/npm/web-vitals@3`) is a runtime dependency that could fail if CDN is unreachable. |
| **Root Cause** | Web Vitals are loaded from an external CDN rather than bundled. |
| **Severity** | **Low** |
| **Fix Difficulty** | Easy — bundle web-vitals as a proper npm dependency |

### 6.2 Sentry Configuration Files Present But No DSN Configured ⚠️ High

| Field | Value |
|---|---|
| **Files** | `sentry.client.config.ts`, `sentry.edge.config.ts`, `sentry.server.config.ts` |
| **Exact Error** | Sentry init tries `process.env.GLITCHTIP_DSN` or `SENTRY_DSN` — neither is set (missing `.env` file). |
| **Root Cause** | Sentry/GlitchTip monitoring will silently fail at runtime. No monitoring data will be captured. |
| **Severity** | **High** (no error monitoring in production) |
| **Fix Difficulty** | Easy — set `GLITCHTIP_DSN` in `.env` or disable Sentry |

---

## 7. Type Definition Concerns

### 7.1 Inconsistent Null Handling in Type Definitions ⚠️ Medium

| Field | Value |
|---|---|
| **Files** | `src/types/dialects.ts`, `src/types/vocabulary.ts`, `src/types/streak.ts` |
| **Exact Error** | Type definitions use `string | undefined` throughout, but Drizzle queries return `string | null`. This causes type errors in ~50+ locations. |
| **Root Cause** | The custom type definitions were created manually and don't match Drizzle's generated types. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Hard — either switch to Drizzle-generated types or align all definitions to match DB |

### 7.2 Type Definition Inconsistencies in `src/types/learning-paths.ts` ⚠️ Medium

| Field | Value |
|---|---|
| **File** | `src/types/learning-paths.ts:470` |
| **Exact Error** | 2 TypeScript errors at line 470 |
| **Root Cause** | Type definitions for learning paths are out of sync with the Drizzle schema, likely missing fields or wrong types. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Medium |

---

## 8. Runtime Error Risks

### 8.1 Non-null Assertions Without Guards ⚠️ Critical

| Fields | `src/db/index.ts:5`, `src/lib/supabase.ts:3-4`, `src/lib/supabase.ts:10` |
|---|---|
| **Exact Errors** | Uses `process.env.DATABASE_URL!`, `process.env.NEXT_PUBLIC_SUPABASE_URL!`, `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!`, `process.env.SUPABASE_SERVICE_ROLE_KEY!` with non-null assertions |
| **Root Cause** | Since `.env` is missing, all these will be `undefined` at runtime. The `!` assertion won't catch it — the app will crash with obscure errors. |
| **Severity** | **Critical** |
| **Fix Difficulty** | Easy — add runtime validation with clear error messages |

### 8.2 In-Memory Rate Limiting Loses State on Serverless Cold Starts ⚠️ Medium

| Field | Value |
|---|---|
| **File** | `src/lib/auth.ts:306-326` |
| **Exact Error** | Uses `Map<string, { count, resetTime }>` in-memory. Netlify serverless functions can have multiple instances and cold starts, making this rate limiter unreliable. |
| **Root Cause** | The architecture documentation (ARCHITECTURE_REMEDIATION_PLAN.md) acknowledges this is acceptable, but it's still a risk under high load. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Hard — requires external rate limiting (e.g., database-based) |

---

## 9. Test Infrastructure Issues

### 9.1 Jest Configuration Mismatch ⚠️ Medium

| Field | Value |
|---|---|
| **Files** | `package.json` has `jest` and `ts-jest` in devDependencies |
| **Exact Error** | No `jest.config.js` or `jest.config.ts` found in the project root. Tests exist at `src/lib/__tests__/` and `src/types/__tests__/` but cannot run without configuration. |
| **Root Cause** | Jest was installed but never configured. |
| **Severity** | **Medium** |
| **Fix Difficulty** | Easy — create `jest.config.ts` with `ts-jest` preset |

---

## 10. Full File-by-File Error Breakdown

### Source Files with TypeScript Errors

| File | Error Count | Primary Issue |
|---|---|---|
| `src/lib/security/rbac.ts` | 44 | Missing imports/exports |
| `src/lib/placement/analytics/index.ts` | 29 | Type null/undefined mismatch |
| `src/lib/dialects/dialect-service.ts` | 28 | Type null/undefined mismatch |
| `src/lib/search/dialect-search.ts` | 16 | Type null/undefined mismatch |
| `src/lib/community/integration.ts` | 11 | Missing exports + type errors |
| `src/lib/filters/dialect-filters.ts` | 10 | Type null/undefined mismatch |
| `src/lib/xp/rewards.ts` | 10 | Type null/undefined mismatch |
| `src/lib/xp/achievements.ts` | 8 | Type null/undefined mismatch |
| `src/lib/challenges/scoring.ts` | 7 | Missing imports |
| `src/app/admin/community/page.tsx` | 7 | Page props type mismatch |
| `src/lib/audit/audit-logger.ts` | 6 | Schema/code mismatch |
| `src/lib/actions/challenges.ts` | 6 | Type errors |
| `src/lib/xp/xp.ts` | 5 | Type errors |
| `src/lib/comparison/lexical-comparator.ts` | 5 | null/undefined + missing imports |
| `src/app/api/dialects/dictionary/[word]/route.ts` | 5 | Route context type mismatch |
| `src/lib/xp/progression.ts` | 4 | Type errors |
| `src/types/vocabulary.ts` | 4 | Type definition errors |
| Various `.next/types/` files | ~100+ | Next.js 15 params/Promise mismatch |
| **Total source errors (non-.next)** | **~280+** | |

---

## Remediation Priority Matrix

| Issue ID | Severity | Effort | Priority |
|---|---|---|---|
| 1.1 — Missing `.env` | Critical | Easy | **P0 - Fix Now** |
| 8.1 — Non-null assertions | Critical | Easy | **P0 - Fix Now** |
| 2.1 — Next.js 15 params | Critical | Medium | **P0 - Fix Now** |
| 4.1 — Missing locale layout | Critical | Medium | **P0 - Fix Now** |
| 3.1 — Next.js CVE | Critical | Easy | **P0 - Fix Now** |
| 2.2 — null vs undefined | Critical | Medium | **P0 - Fix Now** |
| 3.5 — npm vulnerabilities | High | Medium | **P1** |
| 2.3 — Missing challengeLeaderboards import | High | Easy | **P1** |
| 2.4 — Missing `sql` import | High | Easy | **P1** |
| 2.5 — Missing `addXP` export | High | Medium | **P1** |
| 2.6 — Missing type exports | High | Medium | **P1** |
| 2.7 — Audit logger schema mismatch | High | Medium | **P1** |
| 2.10 — percentile column missing | High | Medium | **P1** |
| 2.11 — coins column missing | High | Medium | **P1** |
| 5.1 — Audit log schema misuse | High | Medium | **P1** |
| 6.2 — Sentry no DSN | High | Easy | **P1** |
| 2.8 — Missing `and` import | Medium | Easy | **P2** |
| 2.9 — Date vs string type | Medium | Easy | **P2** |
| 2.12 — `db.fn` doesn't exist | Medium | Medium | **P2** |
| 2.13 — number vs string numeric | Medium | Easy | **P2** |
| 4.2 — Missing not-found.tsx | Medium | Easy | **P2** |
| 4.3 — Missing error.tsx | Medium | Easy | **P2** |
| 5.2 — Duplicate migration numbers | Medium | Hard | **P2** |
| 7.1 — Type null/undefined in types | Medium | Hard | **P2** |
| 8.2 — In-memory rate limiting | Medium | Hard | **P2** |
| 9.1 — Jest config missing | Medium | Easy | **P2** |
| 3.2 — Deprecated auth-helpers | Low | Easy | **P3** |
| 3.3 — Transitive deprecated dep | Low | Easy | **P3** |
| 3.4 — EOL ESLint | Low | Medium | **P3** |
| 5.3 — Migration numbering gaps | Low | Hard | **P3** |
| 6.1 — CDN-loaded web-vitals | Low | Easy | **P3** |

---

*End of Report — No files were modified during analysis.*